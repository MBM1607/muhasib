#: import Gradient custom_widgets.Gradient

<SalahLabel>:
	spacing: dp(10)
	canvas.before:
		Rectangle:
			pos: self.pos
			size: self.size
			texture: Gradient.vertical(*root.background_colors)

	Label:
		text: root.name

	Label:
		text: root.time

<PrayerOptions>:
	size_hint: 0.5, 0.2
	BoxLayout:
		orientation: "horizontal"
		spacing: dp(5)
		padding: dp(5)
		PrayerOptionsButton:
			text: "Group"
			size_hint_y: 0.5
			pos_hint: {"center_y": 0.5}
		PrayerOptionsButton:
			text: "Alone"
			size_hint_y: 0.5
			pos_hint: {"center_y": 0.5}
		PrayerOptionsButton:
			text: "Delayed"
			size_hint_y: 0.5
			pos_hint: {"center_y": 0.5}
